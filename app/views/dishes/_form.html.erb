<%=form_with model: @dish, local: true do |form| %>
  <div class = "form-erros">
    <% if @dish.errors.any? %>
    <ul>
      <% @dish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
    <% end %>
  </div>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <%
=begin%>
 <div>
  <%= form.label :ingredient_id, 'ingredient' %>
  <%= form.collection_select :ingredient_id, ingredient.all, :id, :name, { selected: @ingredient_id } %>
  </div> 
<%
=end%>

  <div>
    <%= form. submit %> 
  </div>
<% end %>